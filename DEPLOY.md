部署与使用说明

- 本站为静态页面，打开 `index.html` 即可在本地浏览（电脑/手机均可）。
- 推荐部署方式：
  - GitHub Pages：将仓库 push 到 GitHub，设置 Pages 分支为 `main` 或 `gh-pages`，选择根目录或 `/` 即可上线。
  - 任意静态托管：Netlify、Vercel、Coding Pages、七牛、COS 等均可托管静态站点，直接指向仓库或上传构建后的文件。

- 编辑说明：
  - 页面主要内容在 `index.html`，样式在 `styles.css`，交互脚本在 `script.js`。
  - 所有“预留”字段用 `span.editable` 标注，双击页面上的文本可以临时编辑（仅当前浏览器会话，若需永久请修改 `index.html` 源码）。
  - 相册图片与占位图位于 `images/` 文件夹，替换同名文件即可更新展示。

- 表单说明：
  - `参会确认` 表单为静态示例，页面内会弹窗提示。若需收集参会数据，请使用第三方问卷或添加后端接收。

如需我帮你：
- 替换为真实信息并生成最终上线版本；
- 将静态站点部署到 GitHub Pages 并配置域名；
- 或根据需求把表单对接到 Google Forms/腾讯问卷。 
